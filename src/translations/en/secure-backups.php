<?php

/**
 * Secure Backups en translations.
 */

return [
    // Populated as the plugin's UI grows. Craft falls back to the source string,
    // so an untranslated key renders correctly rather than showing the handle.
];
